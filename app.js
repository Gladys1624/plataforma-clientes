(()=>{'use strict';
const DB='PC_DB_V2', STORES=['companies','folders','documents','norms','audit']; let db, companyId=null, folderId=null, subfolderId=null;
const $=s=>document.querySelector(s), esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])), now=()=>new Date().toISOString();
function openDB(){return new Promise((res,rej)=>{const r=indexedDB.open(DB,1);r.onupgradeneeded=()=>{const d=r.result;STORES.forEach(s=>{if(!d.objectStoreNames.contains(s)){const o=d.createObjectStore(s,{keyPath:'id'});if(['folders','documents'].includes(s))o.createIndex('companyId','companyId')}})};r.onsuccess=()=>{db=r.result;res()};r.onerror=()=>rej(r.error)})}
function all(s){return new Promise((res,rej)=>{const r=db.transaction(s).objectStore(s).getAll();r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})}function put(s,x){return new Promise((res,rej)=>{const r=db.transaction(s,'readwrite').objectStore(s).put(x);r.onsuccess=()=>res();r.onerror=()=>rej(r.error)})}
async function audit(action,type,id,detail=''){await put('audit',{id:crypto.randomUUID(),at:now(),user:'gladys',action,type,entityId:id,detail})}
const ROOTS=[['01','ORGANIZACIÓN'],['02','PLANIFICACIÓN'],['03','APLICACIÓN'],['04','VERIFICACIÓN'],['05','MEJORAMIENTO'],['06','POLÍTICAS']];
async function ensureCompanyFolders(c){for(const [code,name] of ROOTS){const id=c.id+'-'+code;const exists=(await all('folders')).some(f=>f.id===id);if(!exists)await put('folders',{id,companyId:c.id,parentId:null,code,name,createdAt:now()})}}
async function migrateOld(){if(localStorage.getItem('PC_MIGRATED_V2'))return;for(const key of ['pc_v8','pc_v7','pc_v6','pc_v5','pc_v4','pc_v3']){try{const x=JSON.parse(localStorage.getItem(key)||'null');if(!x?.companies)continue;for(const c of x.companies){if(!(await all('companies')).some(y=>y.id===c.id))await put('companies',{...c,id:String(c.id),createdAt:c.createdAt||now(),updatedAt:now()});}break}catch(e){}}
const cs=await all('companies');for(const c of cs)await ensureCompanyFolders(c);for(const key of ['pc_v8','pc_v7','pc_v6','pc_v5']){try{const x=JSON.parse(localStorage.getItem(key)||'null');if(!x?.docs)continue;for(const d of x.docs){const id=String(d.id||crypto.randomUUID());if(!(await all('documents')).some(z=>z.id===id))await put('documents',{id,companyId:String(d.companyId||d.company||'wonder'),folderId:String((d.companyId||d.company||'wonder')+'-'+(d.folder||'01')),subfolderId:d.subfolder||null,name:d.name||'Documento',code:d.code||'',type:d.type||'CARGADO',version:d.version||'01',status:d.status||'REGISTRADO',description:d.description||'',createdAt:d.created||now(),updatedAt:now()})}break}catch(e){}}localStorage.setItem('PC_MIGRATED_V2',now())}
async function normalizeData(){let cs=await all('companies');if(!cs.length){const c={id:'wonder',name:'WONDER PACK S.A.S.',city:'Medellín',employees:30,risk:'IV',arl:'ARL SURA',createdAt:now(),updatedAt:now()};await put('companies',c);cs=[c]}for(const c of cs)await ensureCompanyFolders(c);await migrateOld();cs=await all('companies');for(const c of cs)await ensureCompanyFolders(c);}
function modal(title,html){$('#modal').innerHTML='<div class="modalbox"><h2>'+title+'</h2>'+html+'<div class="right"><button class="btn" id="close">Cerrar</button></div></div>';$('#modal').style.display='grid';$('#close').onclick=()=>$('#modal').style.display='none'}
async function render(){const cs=await all('companies');if(!cs.length)return;if(!companyId||!cs.some(c=>c.id===companyId))companyId=cs[0].id;const fs=(await all('folders')).filter(f=>f.companyId===companyId);const roots=fs.filter(f=>!f.parentId).sort((a,b)=>a.code.localeCompare(b.code));if(!folderId||!fs.some(f=>f.id===folderId))folderId=roots[0]?.id;const children=fs.filter(f=>f.parentId===folderId);if(subfolderId&&!children.some(f=>f.id===subfolderId))subfolderId=null;const c=cs.find(x=>x.id===companyId),f=fs.find(x=>x.id===folderId),s=fs.find(x=>x.id===subfolderId);const ds=(await all('documents')).filter(d=>d.companyId===companyId);$('#company').innerHTML=cs.map(x=>'<option value="'+esc(x.id)+'">'+esc(x.name)+'</option>').join('');$('#company').value=companyId;$('#nav').innerHTML=roots.map(r=>'<button class="navitem '+(r.id===folderId&&!subfolderId?'sel':'')+'" data-folder="'+esc(r.id)+'">'+esc(r.code)+' · '+esc(r.name)+' <b>'+ds.filter(d=>d.folderId===r.id).length+'</b></button>'+(r.id===folderId?'<button class="newsub" id="newsub">＋ Nueva subcarpeta</button>'+children.map(ch=>'<button class="subitem '+(ch.id===subfolderId?'sel':'')+'" data-sub="'+esc(ch.id)+'">📁 '+esc(ch.name)+' <b>'+ds.filter(d=>d.subfolderId===ch.id).length+'</b></button>').join(''):'')).join('');const list=ds.filter(d=>d.folderId===folderId&&(!subfolderId||d.subfolderId===subfolderId));$('#content').innerHTML='<div class="hero"><div><small>'+esc(f?.code||'')+' · '+esc(f?.name||'')+(s?' / '+esc(s.name):'')+'</small><h1>'+esc(c.name)+'</h1><p>'+esc(c.city||'')+' · '+(c.employees||0)+' trabajadores · Riesgo '+esc(c.risk||'—')+' · '+esc(c.arl||'—')+'</p></div><div><button class="btn" id="newdoc">＋ Crear documento</button> <button class="btn" id="upload">📎 Subir documento</button> <button class="btn primary" id="ai">✦ IA</button></div></div><div class="stats"><div><small>TRABAJADORES</small><strong>'+(c.employees||0)+'</strong></div><div><small>CARPETAS</small><strong>6</strong></div><div><small>SUBCARPETAS</small><strong>'+fs.filter(x=>x.parentId).length+'</strong></div><div><small>DOCUMENTOS</small><strong>'+ds.length+'</strong></div></div><section class="panel"><header><h2>📁 '+esc(s?.name||f?.name||'Expediente')+'</h2><p>Empresa → carpeta → subcarpeta → documentos.</p></header><table><thead><tr><th>CÓDIGO</th><th>DOCUMENTO</th><th>TIPO</th><th>VERSIÓN</th><th>ESTADO</th></tr></thead><tbody>'+(list.length?list.map(d=>'<tr><td>'+esc(d.code)+'</td><td>'+esc(d.name)+'</td><td>'+esc(d.type)+'</td><td>'+esc(d.version)+'</td><td>'+esc(d.status)+'</td></tr>').join(''):'<tr><td colspan="5" class="empty">No hay documentos aquí. Usa “Crear documento” o “Subir documento”.</td></tr>')+'</tbody></table></section><section class="panel"><header><h2>CONTROL Y SEGURIDAD</h2><p>Información y referencias de la empresa.</p></header><div class="cards"><button class="cardbtn" id="backup">💾 Descargar respaldo completo</button><button class="cardbtn" id="restore">↩ Restaurar respaldo</button><button class="cardbtn" id="norms">⚖ Biblioteca normativa</button></div></section>'}
async function newCompany(){modal('🏢 Nueva empresa','<input id="cn" placeholder="Razón social"><input id="ci" placeholder="NIT"><input id="cc" placeholder="Ciudad"><input id="ce" type="number" placeholder="Trabajadores"><select id="cr"><option>I</option><option>II</option><option>III</option><option>IV</option><option>V</option></select><input id="ca" placeholder="ARL"><div class="right"><button class="btn primary" id="savec">Crear empresa</button></div>');$('#savec').onclick=async()=>{const name=$('#cn').value.trim();if(!name)return alert('Escribe la razón social.');const id=crypto.randomUUID(),c={id,name,nit:$('#ci').value.trim(),city:$('#cc').value.trim(),employees:+$('#ce').value||0,risk:$('#cr').value,arl:$('#ca').value.trim(),createdAt:now(),updatedAt:now()};await put('companies',c);await ensureCompanyFolders(c);companyId=id;folderId=id+'-01';subfolderId=null;$('#modal').style.display='none';await audit('CREAR','empresa',id,name);render()}}
async function newSub(){const f=(await all('folders')).find(x=>x.id===folderId);modal('📁 Nueva subcarpeta','<p>Dentro de <b>'+esc(f?.name)+'</b></p><input id="sn" placeholder="Ej.: 01.01 Información general"><div class="right"><button class="btn primary" id="saves">Crear</button></div>');$('#saves').onclick=async()=>{const n=$('#sn').value.trim();if(!n)return alert('Escribe el nombre.');const id=crypto.randomUUID();await put('folders',{id,companyId,parentId:folderId,code:'',name:n,createdAt:now()});subfolderId=id;$('#modal').style.display='none';await audit('CREAR','subcarpeta',id,n);render()}}
async function newDoc(){modal('📄 Nuevo documento','<input id="dc" placeholder="Código documental"><input id="dn" placeholder="Nombre del documento"><select id="dt"><option>Procedimiento</option><option>Programa</option><option>Plan</option><option>Política</option><option>Formato</option><option>Acta</option><option>Informe</option><option>Matriz</option><option>Manual</option></select><textarea id="dd" placeholder="Descripción / requisitos"></textarea><div class="right"><button class="btn primary" id="saved">Guardar documento</button></div>');$('#saved').onclick=async()=>{const name=$('#dn').value.trim();if(!name)return alert('Escribe el nombre.');const id=crypto.randomUUID();await put('documents',{id,companyId,folderId,subfolderId:subfolderId||null,code:$('#dc').value.trim(),name,type:$('#dt').value,description:$('#dd').value,version:'01',status:'BORRADOR',createdAt:now(),updatedAt:now()});$('#modal').style.display='none';await audit('CREAR','documento',id,name);render()}}
async function upload(){const i=document.createElement('input');i.type='file';i.multiple=true;i.onchange=async()=>{for(const file of i.files){const id=crypto.randomUUID();await put('documents',{id,companyId,folderId,subfolderId:subfolderId||null,code:'ARCHIVO',name:file.name,type:'ARCHIVO',version:'01',status:'CARGADO',description:'Archivo cargado: '+file.type,createdAt:now(),updatedAt:now()});await audit('CARGAR','documento',id,file.name)}render()};i.click()}
async function backup(){const out={format:'PLATAFORMA_CLIENTES_BACKUP_V2',createdAt:now()};for(const s of STORES)out[s]=await all(s);const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(out,null,2)],{type:'application/json'}));a.download='PLATAFORMA_CLIENTES_RESPALDO_'+new Date().toISOString().slice(0,10)+'.json';a.click();URL.revokeObjectURL(a.href)}
function restore(){const i=document.createElement('input');i.type='file';i.accept='.json';i.onchange=async()=>{try{const d=JSON.parse(await i.files[0].text());if(!d.format?.startsWith('PLATAFORMA_CLIENTES_BACKUP'))throw Error('Formato no reconocido');for(const s of STORES)for(const x of d[s]||[])await put(s,x);alert('Respaldo restaurado correctamente.');await render()}catch(e){alert('No se pudo restaurar: '+e.message)}};i.click()}
async function norms(){const ns=await all('norms');modal('⚖ Biblioteca normativa','<p>Las referencias se controlarán por vigencia, aplicabilidad y fuente oficial.</p>'+(ns.length?ns.map(n=>'<div class="norm"><b>'+esc(n.code)+'</b> · '+esc(n.title)+'<br><small>'+esc(n.status||'Vigente')+'</small></div>').join(''):'<div class="empty">Biblioteca normativa lista para incorporar la matriz normativa verificada.</div>'))}
function ai(){modal('🤖 Asistente IA','<p>Solicitud vinculada a la empresa, carpeta y subcarpeta actuales.</p><textarea id="aq" placeholder="Ej.: crea el procedimiento de inspecciones de seguridad y sus referencias normativas aplicables"></textarea><div class="right"><button class="btn primary" id="ask">Preparar solicitud</button></div><div id="out"></div>');$('#ask').onclick=()=>$('#out').innerHTML='<div class="norm">Solicitud preparada. La conexión de IA se realizará mediante backend seguro; no se deben guardar claves de IA en el navegador.</div>'}
document.addEventListener('click',e=>{const t=e.target;if(t.id==='newcompany')newCompany();else if(t.dataset.folder){folderId=t.dataset.folder;subfolderId=null;render()}else if(t.dataset.sub){subfolderId=t.dataset.sub;render()}else if(t.id==='newsub')newSub();else if(t.id==='newdoc')newDoc();else if(t.id==='upload')upload();else if(t.id==='ai')ai();else if(t.id==='backup')backup();else if(t.id==='restore')restore();else if(t.id==='norms')norms()});document.addEventListener('change',e=>{if(e.target.id==='company'){companyId=e.target.value;folderId=companyId+'-01';subfolderId=null;render()}});
(async()=>{await openDB();await normalizeData();$('#login').innerHTML='<div class="loginbox"><div class="brand"><strong>PLATAFORMA CLIENTES</strong><small>Gestión integral empresarial</small></div><h1>Ingreso privado</h1><input id="lu" placeholder="Usuario"><input id="lp" type="password" placeholder="Contraseña"><button class="btn primary" id="loginbtn">Ingresar</button><p class="muted">Datos persistentes y respaldo.</p></div>';$('#loginbtn').onclick=()=>{if($('#lu').value==='gladys'&&$('#lp').value==='GladysPro2026!'){sessionStorage.user='gladys';$('#login').style.display='none';$('#app').style.display='block';render()}else alert('Usuario o contraseña incorrectos.')};if(sessionStorage.user==='gladys'){$('#login').style.display='none';$('#app').style.display='block';render()}})();})();